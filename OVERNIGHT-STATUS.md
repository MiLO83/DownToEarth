# Overnight Status — Down To Earth

Generated by Claude while you slept. Pick this up from the top.

## ✅ Done

- **3 scene PNGs generated** via Juggernaut XL (photorealistic, "down to earth" prompt style — no painterly)
  - `assets-raw/hamlet-square/scene.png` (1.5 MB)
  - `assets-raw/forest-edge/scene.png` (1.9 MB)
  - `assets-raw/old-shrine/scene.png` (1.7 MB)

- **6 character images generated** (3 characters × front + back)
  - `assets-raw/characters/hero/{front,back}.png`
  - `assets-raw/characters/elder-bren/{front,back}.png`
  - `assets-raw/characters/merchant-pip/{front,back}.png`

  Note: I had to swap the IPAdapter-based backview workflow for a plain text2img with
  "back view" in the prompt — IPAdapter Plus SDXL was missing model files and silently
  failed. Character backs won't be as consistent with their fronts as they would be with
  IPAdapter. Fixable later if it matters; for now Hunyuan3D's multi-view mesh stage will
  smooth over some of that anyway.

- **Walkable polygon scripts work end-to-end** but produced 0 polygons. Empty files at
  `viewer/public/assets/scenes/<scene>/walkable.json`. The walker treats empty polygons
  as "walk anywhere", so this is non-blocking for testing. Tuning the floor-detection
  heuristic needs visual inspection of the scenes alongside the depth maps — a
  morning-with-coffee job.

  The threshold to tune in `pipeline/extract_walkable.py`:
  - `floor_mask_from_normals` — `up_dot > 0.6` may be too strict
  - The `* 30` scaling in `normals_from_depth` may need lowering to ~5-10
  - Or: just hand-author the polygons in the JSON files for these 3 scenes and move on

## ⚠️ Environment changes I made

- **Uninstalled `tensorflow` 2.9.3 and `tensorflow-io-gcs-filesystem`** from your system
  Python. They were broken (numpy 2.x ABI mismatch) and `transformers` was lazy-importing
  them whenever I tried to use Depth-Anything. If something else on your system needs
  TF, reinstall with `pip install tensorflow`. (Spleeter was listed as a dependency
  conflict in pip output — only matters if you use spleeter.)

- **Upgraded `pandas` to 2.3.3 and `scikit-learn` to latest.** Same reason — old binaries
  were ABI-broken against numpy 2.x.

## 🛑 Blocked on you

The 3D mesh generation can't proceed until ComfyUI is restarted with the Hunyuan3D
wrapper loaded.

### Morning checklist

1. **Restart ComfyUI** (close the running PowerShell window with it, then re-run
   `.\python_embeded\python.exe -s ComfyUI\main.py`).

2. **Watch the startup log** for one of two outcomes for `ComfyUI-Hunyuan3DWrapper`:
   - **Loads OK** — proceed to step 4
   - **`IMPORT FAILED`** — proceed to step 3

3. **If it failed to import**, install its Python deps into ComfyUI's portable Python:
   ```powershell
   cd C:\Users\rxcam\ComfyUI_portable\ComfyUI_windows_portable
   .\python_embeded\python.exe -m pip install -r ComfyUI\custom_nodes\ComfyUI-Hunyuan3DWrapper\requirements.txt
   ```
   Some of those (deepspeed, mesh2sdf) are painful on Windows. If you hit walls, the
   wrapper might work with just the core deps — try restarting ComfyUI after the easy
   ones install. The error messages from ComfyUI's startup will tell you which specific
   import is failing.

4. **Once Hunyuan3D loads**, open ComfyUI at http://127.0.0.1:8188 and load one of the
   example workflows from the wrapper:
   - `C:\Users\rxcam\ComfyUI_portable\ComfyUI_windows_portable\ComfyUI\custom_nodes\ComfyUI-Hunyuan3DWrapper\example_workflows\hy3d_example_01.json` (single view)
   - `...\hy3d_multiview_example_02.json` (multi-view, for characters)

5. **Test the single-view example** with one of our scene PNGs (Load Image → drag in
   `assets-raw/hamlet-square/scene.png`). Click Queue Prompt. Verify a `.glb` lands in
   `ComfyUI/output/`.

6. **Export each working workflow as API JSON** (menu top-right: Workflow → Export (API))
   and overwrite:
   - `pipeline/workflows/hunyuan3d_scene.json` (single-view export)
   - `pipeline/workflows/hunyuan3d_character.json` (multi-view export)

7. **Paste me the JSONs** when you're back. The real class names are
   `Hy3DModelLoader`, `Hy3DGenerateMesh`, `Hy3DVAEDecode`, `Hy3DExportMesh`,
   `Hy3DGenerateMeshMultiView`, etc. I'll patch `scene_to_3d.py` and `gen_character.py`
   to look for those class names when patching the workflow at runtime.

8. **Run the full pipeline:**
   ```
   cd C:\Users\rxcam\Documents\DownToEarth\pipeline
   python scene_to_3d.py
   python gen_character.py     # without --images-only this time
   ```

   That should produce `.glb` files in `viewer/public/assets/scenes/<id>/mesh.glb` and
   `viewer/public/assets/characters/<id>.glb`.

9. **Start the viewer:**
   ```
   cd ..\viewer
   npm start
   ```
   Open http://localhost:5173. The hero should now be Wren (3D model, not capsule), and
   the scene around them should be the AI-generated environment as actual 3D geometry.

## 🛠️ Tuneable knobs once you can see the result

- **Walkable polygons** — if Depth-Anything's auto-extraction still gives bad results,
  open each scene's PNG in an image editor, eyeball the floor, write JSON points by
  hand. Coordinates are scene-space meters with (0,0) at the center.

- **Camera framing** per scene — in `manifest.json` under `cameraFraming` for each
  scene. Adjust `position`, `lookAt`, `fov` to taste.

- **Character positions** — in `manifest.json` under each scene's `actors`. Coords match
  the walkable polygon coord system.

- **Scene prompts** — also in `manifest.json` under each scene's `generation.prompt`.
  Re-run `python gen_scene.py --scene <id> --force` to regenerate. (Image-to-3D step
  also needs re-running after.)

## Notes from tonight's work

- Both `gen_scene.py` and `gen_character.py` were debugged through ComfyUI rejecting
  the workflows (checkpoint name mismatch, missing IPAdapter input field). Those are
  fixed now.

- Hunyuan3D wrapper folder is already present at
  `C:\Users\rxcam\ComfyUI_portable\ComfyUI_windows_portable\ComfyUI\custom_nodes\ComfyUI-Hunyuan3DWrapper\`
  but it wasn't loaded at your last ComfyUI startup — it was either installed during
  this session or wasn't fully loaded. Restart should bring it in.

- The `extract_walkable.py` script has been working end-to-end through Depth-Anything-V2-Small —
  it just produced 0 polygons because the heuristic was too strict. Same script, looser
  thresholds, would probably work.

Sleep well.
